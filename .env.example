# ── Portfolio — Environment Variables ────────────────
# Copy this file to .env.local and adjust for your setup.
# Backend-specific env vars live in backend/.env — see backend/.env.example

# (Root-level env vars go here if needed in the future)
